<div class='article-image'>
  <%= image_tag @news.file.url %>
</div>
<div class='article-title'>
  <h2><%= @news.title %></h2>
</div>
<div class='article-content'>
  <p><%= @news.content.html_safe %></p>
  <%= raw @news.tags.map { |t| link_to t.name, news_path(tag: t.name) }.join(', ') %>
</div>
